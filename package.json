{
  "name": "@vibearound/plugin-channel-qqbot",
  "version": "0.6.3",
  "private": true,
  "type": "module",
  "description": "VibeAround QQ Bot (v2) channel plugin (stdio JSON-RPC)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "dev": "tsc --watch",
    "start": "node dist/main.js",
    "postinstall": "echo 'Note: openclaw peer dependency warning is expected and safe to ignore.'"
  },
  "dependencies": {
    "@tencent-connect/openclaw-qqbot": "^1.7.2",
    "@vibearound/plugin-channel-sdk": "^0.6.2",
    "ws": "^8.21.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
